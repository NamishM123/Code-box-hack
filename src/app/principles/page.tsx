import { LibraryPage } from "@/components/LibraryPage";

export default function PrinciplesPage() {
  return <LibraryPage title="Principles we place with" intro="Sightline turns time-tested spatial rules into useful, inspectable decisions for your room." />;
}
